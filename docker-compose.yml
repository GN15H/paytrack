services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: paytrack
      POSTGRES_PASSWORD: paytrack123
      POSTGRES_DB: paytrack
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
